if [ -z "${global.board.display}" ]; then
  exit
fi

if [ -e /env/splash.png ]; then
  fb0.mode_name="${global.board.display}"
  splash -b 0 /env/splash.png
  fb0.enable=1
  backlight0.brightness="${state.display.brightness}"
fi
